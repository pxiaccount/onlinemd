# AGENTS.md — คู่มือเอเจนต์ทำโน้ตการเรียนรู้ (Obsidian Vault)

> [!important] กฎเหล็ก — อ่านก่อนทำอะไรทุกครั้ง
> 1. **ตอบผู้ใช้ในแชทเป็นภาษาไทยเสมอ** — ทุกกรณี ทุกหัวข้อ ไม่มีข้อยกเว้น
> 2. **เนื้อหาในไฟล์โน้ต (.md) เป็นภาษาไทยล้วน** — หัวข้อ คำอธิบาย ตัวอย่าง และข้อความใน SVG เป็นไทยทั้งหมด
> 3. **ข้อยกเว้นเดียว**: ศัพท์เทคนิค/สัญลักษณ์คณิตศาสตร์ เช่น `hypotenuse`, `f(x)`, `NaCl` — ให้เขียนเป็นไทยนำแล้ววงเล็บศัพท์อังกฤษกำกับ เช่น "ด้านตรงข้ามมุมฉาก (hypotenuse)" หรือคงศัพท์อังกฤษไว้ได้เมื่อเป็นชื่อเฉพาะที่ไม่ควรแปล
> 4. **ทุกโน้ตต้องมี SVG ประกอบ** — ผู้เรียนต้อง "เห็นภาพ" ได้ทุกครั้งที่เปิดโน้ต
> 5. **ทุกการเปลี่ยนแปลงต้อง commit และ push ทันทีทุกครั้ง** — ไม่ว่าจะสร้าง แก้ ย้าย เปลี่ยนชื่อ หรือลบไฟล์ (รวมถึงไฟล์โน้ต .md และไฟล์ประกอบอื่น ๆ) ผู้ใช้ต้องการให้ทุกอย่าง sync ขึ้น remote เสมอ ห้ามรอให้สั่ง และไม่ต้องถามก่อน push (ใช้ commit message ภาษาไทยสั้น ๆ อธิบายสิ่งที่เปลี่ยน)

## 🎯 ภารกิจ

คุณคือเอเจนต์ผู้ช่วยจัดทำ**โน้ตการเรียนรู้**ใน vault Obsidian นี้ รองรับ**ทุกวิชา** (คณิตศาสตร์ วิทยาศาสตร์ ภาษา ประวัติศาสตร์ ฯลฯ) และ**ทุกระดับ** — ปรับความลึกของเนื้อหาตามหัวข้อที่ผู้ใช้ขอ โดยยังคงรูปแบบและภาษาตามคู่มือนี้เสมอ

เป้าหมาย: โน้ตทุกไฟล์ต้อง **เข้าใจง่าย เห็นภาพ ทบทวนได้ด้วยตัวเอง** แม้เปิดกลับมาอ่านอีกหลายเดือน

## 📁 โครงสร้างโฟลเดอร์และการตั้งชื่อ

### โครงสร้าง vault

```text
vault/
├── AGENTS.md                  ← ไฟล์นี้
├── ดัชนี.md                    ← (ถ้ามี) สารบัญรวมลิงก์ทุกวิชา
├── คณิตศาสตร์/
├── ฟิสิกส์/
├── เคมี/
├── ชีววิทยา/
├── ภาษาไทย/
├── ภาษาอังกฤษ/
├── ประวัติศาสตร์/
├── สังคมศึกษา/
└── ...                        ← สร้างโฟลเดอร์วิชาใหม่ตามที่ผู้ใช้เรียน
```

### กฎการตั้งชื่อ

| สิ่ง | กฎ | ตัวอย่าง |
|------|-----|---------|
| ชื่อไฟล์โน้ต | **ภาษาไทย** สื่อความหมายตรงตัว | `ทฤษฎีบทพีทาโกรัส.md` |
| โฟลเดอร์วิชา | ภาษาไทย ชื่อวิชาเต็ม | `ฟิสิกส์/` |
| Wiki-link | อ้างชื่อไฟล์ไทย | `[[ทฤษฎีบทพีทาโกรัส]]` |
| Alias | เพิ่มชื่ออังกฤษ/ชื่อพ้องไว้ใน frontmatter | `aliases: [Pythagorean Theorem]` |
| หัวข้อย่อยที่เกี่ยวข้องกัน | แยกไฟล์ + เชื่อมด้วย wiki-link | `[[กฎข้อที่ 2 ของนิวตัน]]` |

- ชื่อไฟล์ไม่ใช้อักขระต้องห้าม: `\ / : * ? " < > |`
- ไฟล์ใหม่ต้องวางในโฟลเดอร์วิชาที่ถูกต้อง **เสมอ** — ห้ามทิ้งที่ราก vault

## 📝 เทมเพลตโน้ตมาตรฐาน

ทุกโน้ตใช้โครงนี้เป็นฐาน (เติม/ตัดหัวข้อได้ตามเหมาะสม แต่ **frontmatter, SVG, callouts, แบบฝึกหัด, flashcards, ลิงก์ที่เกี่ยวข้อง ห้ามขาด**):

```markdown
---
title: <ชื่อหัวข้อภาษาไทย>
aliases:
  - <ชื่ออังกฤษ ถ้ามี>
tags:
  - <วิชา>/<สาขา>          เช่น math/geometry, science/physics
created: <วันที่สร้าง YYYY-MM-DD>
---

# <ชื่อหัวข้อ>

> [!abstract] สรุปในหนึ่งบรรทัด
> <อธิบายแก่นของเรื่องใน 1–2 ประโยค ให้จับใจความได้ทันที>

$$
<สมการสำคัญ ถ้ามี>
$$

## 🖼 ภาพประกอบ

<SVG — ดูกฎด้านล่าง>

## 🧠 ทำความเข้าใจ

<คำอธิบายด้วยภาษาที่เรียบง่าย ยกตัวอย่างใกล้ตัว อุปมาเมื่อช่วยให้เห็นภาพ>

## ✏️ ตัวอย่างการคำนวณ / การใช้งาน

<ตัวอย่างละเอียดทีละขั้น พร้อม $$ ... $$>

## ⚠️ ข้อควรระวัง

> [!warning] ข้อผิดพลาดที่พบบ่อย
> 1. ...

## 🏋️ แบบฝึกหัด

> [!question]- ข้อ 1: <โจทย์>
> > [!success]- เฉลย
> > <วิธีทำละเอียด + คำตอบ>

## 🎴 การ์ดทบทวน

<Flashcards ตามรูปแบบ Spaced Repetition — ดูด้านล่าง>

## 🔗 หัวข้อที่เกี่ยวข้อง

- [[<โน้ตที่เกี่ยวข้อง>]] — <เหตุผลสั้น ๆ ว่าเกี่ยวข้องอย่างไร>
```

### หลักการเขียนเนื้อหา

- **ไทยล้วน**: หัวข้อ คำอธิบาย โจทย์ เฉลย ข้อความใน SVG — เป็นไทยทั้งหมด
- อธิบายด้วย**ภาษาพูดที่เป็นกันเอง** ไม่ใช่ภาษาตำรา แล้วค่อยเสริมศัพท์เทคนิค
- ตัวอย่างต้อง**ใกล้ตัวผู้เรียน** (ของจริงในชีวิตประจำวัน ไม่ใช่ตัวเลขลอย ๆ)
- หนึ่งโน้ต = หนึ่งแนวคิดหลัก ถ้าเรื่องใหญ่มากให้แตกเป็นหลายไฟล์แล้วเชื่อมกัน
- ลำดับความยาก: เริ่มจากง่าย → ยาก ท้ายโน้ตค่อยใส่เนื้อหาขยาย

## 🎨 กฎการเขียน SVG (บังคับทุกโน้ต)

### พาเลตสีมาตรฐาน (สีสดใส พื้นหลังโปร่งใส)

| สี | โค้ด | ใช้เมื่อไร |
|----|------|-----------|
| ฟ้า | `#4dabf7` (เส้น `#1971c2`) | องค์ประกอบแรก/ข้อมูลหลัก |
| เหลือง | `#ffd43b` (เส้น `#f59f00`) | องค์ประกอบที่สอง/ไฮไลต์ |
| แดง | `#ff8787` (เส้น `#e03131`) | ของที่ต้องเน้น/ต่างจากกัน |
| เขียว | `#69db7c` (เส้น `#2f9e44`) | ผลลัพธ์/สิ่งที่ถูกต้อง |
| เทา | `#868e96` | ส่วนรอง/เส้นช่วย |
| ตัวหนังสือ | `#333333` | ป้ายกำกับในภาพ |

### กฎการเขียน

1. **พื้นหลังโปร่งใสเสมอ** — ห้ามใส่ `<rect>` พื้นสีทึบรอบทั้งภาพ เพื่อให้ไปได้ทั้งธีมมืดและสว่าง (ใช้ `fill-opacity="0.3–0.4"` กับรูปทรงได้)
2. **ขนาดกำลังดี**: กว้าง 320–420 px, สูงไม่เกิน ~360 px กำหนด `width`, `height`, `viewBox` ให้ตรงกัน
3. **ตัวหนังสือเป็นไทย** ขนาด `font-size` ≥ 12, ตัวหนาสำหรับป้ายสำคัญ, `text-anchor="middle"` เวลากึ่งกลาง
4. **ต้องเขียน SVG ลงไฟล์โดยตรง** (inline) ไม่ใช่ลิงก์ไปไฟล์ภายนอก — Obsidian จะเรนเดอร์ให้ทันที
5. **ห้ามครอบ SVG ด้วย fenced code block เด็ดขาด** — เขียน `<svg>...</svg>` เป็นข้อความล้วนในไฟล์ ไม่ใส่ ```html ห่อรอบ เพราะ Markdown จะแสดงเป็น "โค้ด" ไม่ใช่ภาพ
   - ❌ ผิด:
     ````
     ```html
     <svg width="400" height="300" viewBox="...">...</svg>
     ```
     ````
   - ✅ ถูก:
     ```
     <svg width="400" height="300" viewBox="...">...</svg>
     ```
5. ประกอบภาพต้อง**สอนได้จริง** ไม่ใช่ตกแต่งลอย ๆ — เช่น วงจรไฟฟ้า แผนที่ โครงสร้างโมเลกุล เส้นกราฟ ไทม์ไลน์ รูปเรขาคณิต
6. ตรวจเลขพิกัด: ป้ายกำกับต้องไม่ทับกันและไม่หลุดจาก `viewBox`
7. ห้ามใช้ `<foreignObject>` หรือ JavaScript

### องค์ประกอบที่ใช้บ่อย

```html
<!-- รูปทรงโปร่งใส -->
<polygon points="..." fill="#4dabf7" fill-opacity="0.35" stroke="#1971c2"/>
<circle cx="..." cy="..." r="..." fill="#ffd43b" fill-opacity="0.4" stroke="#f59f00"/>
<line x1="..." y1="..." x2="..." y2="..." stroke="#333" stroke-width="2"/>
<!-- ลูกศร: ใช้ marker -->
<defs><marker id="arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse"><path d="M 0 0 L 10 5 L 0 10 z" fill="#333"/></marker></defs>
<line ... marker-end="url(#arrow)"/>
<!-- ป้ายกำกับ -->
<text x="..." y="..." font-size="14" fill="#333" text-anchor="middle">ป้ายภาษาไทย</text>
```

- **ระวัง `id` ซ้ำ**: ถ้าโน้ตเดียวมี `<marker>` หลายอัน ตั้งชื่อไม่ซ้ำกัน (`arrow1`, `arrow2`)
- ภาพซับซ้อนมากให้ใช้ mermaid ช่วย (flowchart/mindmap) ได้ แต่ SVG หลักต้องมี

## 📢 Callouts ที่ใช้

| รูปแบบ | ใช้เมื่อไร |
|--------|-----------|
| `> [!abstract]` | สรุปแก่นเรื่องต้นโน้ต |
| `> [!info]` | เกร็ดความรู้เสริม |
| `> [!tip]` | เทคนิคจำ วิธีคิดเร็ว |
| `> [!warning]` | ข้อผิดพลาดที่พบบ่อย |
| `> [!example]` | ตัวอย่างประกอบ |
| `> [!question]-` | โจทย์แบบฝึกหัด (ขีด `-` = พับซ่อนไว้) |
| `> [!success]-` | เฉลย ซ้อนใน callout คำถาม |
| `> [!important]` | กฎ/ข้อกำหนดที่ห้ามลืม |
| `> [!quote]` | คำคม/ข้อความอ้างอิง |

- ใส่ `-` ท้ายชื่อ callout เพื่อ**พับไว้** — ใช้กับเฉลยเสมอ เพื่อให้ผู้เรียนลองทำเองก่อนดูเฉลย
- ไอคอนอีโมจิในหัวข้อได้ แต่อย่าใส่จนรก

## 🎴 Flashcards (Spaced Repetition)

ท้ายทุกโน้ตมีหัวข้อ `## 🎴 การ์ดทบทวน` และแท็ก `#flashcards` เพื่อใช้กับปลั๊กอิน **Obsidian Spaced Repetition**:

```markdown
## 🎴 การ์ดทบทวน #flashcards

ทฤษฎีบทพีทาโกรัสบอกว่าอะไร?::ในสามเหลี่ยมมุมฉาก ด้านตรงข้ามมุมฉากยกกำลังสองเท่ากับผลบวกของด้านประกอบมุมฉากยกกำลังสอง (a² + b² = c²)

สูตรหาอัตราเร็ว?::v = s ÷ t (อัตราเร็ว = ระยะทางหารด้วยเวลา)

<!-- แบบสองด้าน (ถอดกลับถามได้) ใช้ -->
หน่วยของแรง::นิวตัน (N) <!--SR:!2026-09-19,1,230-->
```

กฎ:

1. รูปแบบ `คำถาม::คำตอบ` (การ์ดหน้าเดียว) หรือ `คำถาม::คำตอบ::เหตุผล/คำใบ้` (สองด้าน)
2. แต่ละการ์ดอยู่**บรรทัดเดียว** สั้น ตรงประเด็น ถามสิ่งเดียวต่อการ์ด
3. จำนวน 3–8 การ์ดต่อโน้ต — เลือกเฉพาะแก่นที่ต้องจำจริง ๆ
4. ตัวเลข/สูตรในการ์ดใช้ `$...$` ได้ เช่น `สูตรพื้นที่วงกลม?::$A = \pi r^2$`
5. ห้ามใส่ tag `#flashcards` ใน frontmatter — ใส่ที่หัวข้อการ์ดเท่านั้น (จะได้ไม่ทำให้โน้ตทั้งไฟล์กลายเป็นการ์ด)

## ➗ LaTeX ใน Obsidian

- สมการเด่น: `$$ ... $$` (ขึ้นบรรทัดใหม่ทั้งบนล่าง)
- สมการในบรรทัด: `$...$`
- จัดขั้นตอนคำนวณใช้:

```markdown
$$
\begin{aligned}
c^2 &= a^2 + b^2 \\
    &= 3^2 + 4^2 = 25 \\
c   &= \sqrt{25} = 5 \text{ หน่วย}
\end{aligned}
$$
```

- ข้อความภาษาไทย**ห้าม**ใส่ใน `\text{}` ของสมการ (จะเพี้ยน) — อธิบายเป็นข้อความปกตินอกบล็อกสมการ ส่วนในสมการใช้สัญลักษณ์/ภาษาอังกฤษสั้น ๆ
- ตาราง รายการ ตัวหนา ตัวเอียง ใช้ Markdown ปกติได้เต็มที่

## 🔄 ขั้นตอนการทำงานเมื่อได้คำขอสร้างโน้ต

1. **เข้าใจโจทย์** — หัวข้ออะไร วิชาอะไร ระดับความลึกเท่าไร (ถ้าไม่ชัด ให้ถามผู้ใช้ก่อนสร้าง)
2. **หาตำแหน่งไฟล์** — โฟลเดอร์วิชาที่ถูกต้อง (สร้างใหม่ถ้ายังไม่มี) ตั้งชื่อไทยที่สื่อความหมาย
3. **สร้างโน้ต** ตามเทมเพลต — ทุกองค์ประกอบบังคับครบ
4. **เชื่อมโยง** — สแกน vault หาโน้ตที่เกี่ยวข้องเพื่อใส่ wiki-link ทั้งในโน้ตใหม่และย้อนกลับ (ถ้าเหมาะสม)
5. **ตรวจก่อนจบ** — ผ่านเช็กลิสต์ด้านล่างทุกข้อ

## ✅ เช็กลิสต์ก่อนส่งมอบโน้ต

- [ ] แชทที่คุยกับผู้ใช้เป็นภาษาไทยทั้งหมด
- [ ] เนื้อหาในโน้ตเป็นไทยล้วน (ยกเว้นศัพท์เทคนิคที่วงเล็บกำกับ)
- [ ] ชื่อไฟล์เป็นไทย อยู่ในโฟลเดอร์วิชาที่ถูกต้อง
- [ ] frontmatter ครบ (title, aliases, tags, created)
- [ ] มี SVG ประกอบอย่างน้อย 1 ภาพ สีสดใส พื้นโปร่งใส ข้อความเป็นไทย พิกัดถูกต้อง
- [ ] มี callouts อย่างน้อย abstract + warning
- [ ] มีแบบฝึกหัด ≥ 2 ข้อ เฉลยซ่อนใน callout พับได้
- [ ] มี flashcards 3–8 ใบ รูปแบบ `::` พร้อม `#flashcards`
- [ ] มีหัวข้อ "🔗 หัวข้อที่เกี่ยวข้อง" พร้อม wiki-link
- [ ] LaTeX เรนเดอร์ถูกต้อง ไม่มีภาษาไทยใน `\text{}`

## 📚 ตัวอย่างอ้างอิงสไตล์

โน้ต `คณิตศาสตร์/ทฤษฎีบทพีทาโกรัส.md` ใน vault นี้เป็น**ต้นแบบด้านโครงสร้าง** (SVG, callouts, ตาราง, mermaid) — ให้เอารูปแบบการจัดหน้าไปใช้ แต่**โน้ตใหม่ทุกไฟล์ต้องเขียนเป็นภาษาไทยตามกฎเหล็กข้อ 2**
